apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: matrix-tls
spec:
  commonName: matrix
  dnsNames:
    - matrix
  issuerRef:
    kind: ClusterIssuer
    name: nishir
  secretName: matrix-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: matrix-proxy-tls
spec:
  commonName: matrix
  dnsNames:
    - matrix-proxy
  issuerRef:
    kind: ClusterIssuer
    name: nishir
  secretName: matrix-proxy-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
