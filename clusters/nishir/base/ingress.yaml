---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nishir-flux-webhook
  namespace: flux-system
spec:
  defaultBackend:
    service:
      name: webhook-receiver
      port:
        number: 80
  tls:
    - hosts:
        - nishir-flux-webhook
