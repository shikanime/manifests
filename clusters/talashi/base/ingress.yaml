apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: talashi-flux-webhook
  namespace: flux-system
spec:
  defaultBackend:
    service:
      name: webhook-receiver
      port:
        number: 80
  tls:
    - hosts:
        - talashi-flux-webhook
