apiVersion: v1
kind: Service
metadata:
  name: sonarqube
  labels:
    app: sonarqube
spec:
  clusterIP: None
  ports:
    - name: web
      port: 9000
      targetPort: web
  selector:
    app: sonarqube
---
apiVersion: v1
kind: Service
metadata:
  name: sonarqube-headless
  labels:
    app: sonarqube
spec:
  clusterIP: None
  ports:
    - name: web
      port: 9000
      targetPort: web
  selector:
    app: sonarqube
