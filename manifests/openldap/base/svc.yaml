apiVersion: v1
kind: Service
metadata:
  name: openldap
spec:
  ports:
    - port: 389
      targetPort: ldap
      name: ldap
    - port: 636
      targetPort: ldaps
      name: ldaps
  selector:
    app: openldap
---
apiVersion: v1
kind: Service
metadata:
  name: openldap-headless
spec:
  clusterIP: None
  ports:
    - port: 389
      targetPort: ldap
      name: ldap
    - port: 636
      targetPort: ldaps
      name: ldaps
  selector:
    app: openldap
