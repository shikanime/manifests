apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-provisioner-export
  namespace: nfs-provisioner
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
