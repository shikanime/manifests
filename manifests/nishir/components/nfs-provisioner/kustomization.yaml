apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
commonLabels:
  app.kubernetes.io/instance: nfs-provisioner-nishir
resources:
  - pvc.yaml
patchesStrategicMerge:
  - patch-deployment.yaml
  - patch-pvc.yaml
