apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  labels:
    app.kubernetes.io/name: nishir
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: torrent-data
  labels:
    app.kubernetes.io/name: nishir
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
