apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-ganesha-export
  labels:
    app.kubernetes.io/name: nishir
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
