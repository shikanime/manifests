apiVersion: v1
kind: Service
metadata:
  name: transmission
  labels:
    app.kubernetes.io/name: transmission
    app.kubernetes.io/version: 3.00-r2
spec:
  type: ClusterIP
  ports:
    - port: 9091
      targetPort: http
      protocol: TCP
      name: http
    - port: 51413
      targetPort: utp
      protocol: TCP
      name: utp
    - port: 51413
      targetPort: utp-udp
      protocol: UDP
      name: utp-udp
  selector:
    app.kubernetes.io/name: transmission
    app.kubernetes.io/version: 3.00-r2
