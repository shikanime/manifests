apiVersion: apps/v1
kind: Deployment
metadata:
  name: hasura
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: hasura
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: prefect-server-postgresql
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: prefect-server-postgresql
                  key: password
            - name: HASURA_GRAPHQL_DATABASE_URL
              value: postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@prefect-server-postgresql/prefect_server
