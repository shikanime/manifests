apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoriametrics
  namespace: flux-system
spec:
  postRenderers:
    - kustomize:
        patches:
          - patch: |
              - op: add
                path: /spec/tls
                value:
                  - hosts:
                      - talashi-telemetry
            target:
              name: vminsert-vmks-victoria-metrics-k8s-stack
              version: v1
              kind: Ingress
  values:
    grafana:
      persistence:
        storageClassName: talashi-standard
    vmcluster:
      spec:
        retentionPeriod: 30d
        vminsert:
          extraArgs:
            enableTCP6: "true"
          replicaCount: 2
        vmselect:
          extraArgs:
            enableTCP6: "true"
          replicaCount: 2
          storage:
            volumeClaimTemplate:
              spec:
                resources:
                  requests:
                    storage: 2Gi
                storageClassName: talashi-standard
        vmstorage:
          extraArgs:
            enableTCP6: "true"
          replicaCount: 2
          storage:
            volumeClaimTemplate:
              spec:
                resources:
                  requests:
                    storage: 8Gi
                storageClassName: talashi-standard
      enabled: true
      ingress:
        insert:
          annotations:
            tailscale.com/tags: tag:web
          enabled: true
          hosts:
            - talashi-telemetry
          ingressClassName: tailscale
    vmsingle:
      enabled: false
