apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-cluster-api-config
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  deletionPolicy: Orphan
  dependsOn:
    - name: infrastructure-cluster-api
  interval: 10m
  path: ./configs/cluster-api/overlays/telsha
  prune: true
  sourceRef:
    name: flux-system
    kind: GitRepository
