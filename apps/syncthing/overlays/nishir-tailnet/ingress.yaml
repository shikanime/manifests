apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: syncthing
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: syncthing
      port:
        name: https
  ingressClassName: tailscale
  tls:
    - hosts:
        - syncthing
