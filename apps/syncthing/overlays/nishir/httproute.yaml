apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: syncthing
spec:
  parentRefs:
    - name: nishir
      sectionName: https
  rules:
    - backendRefs:
        - name: syncthing
          port: 8384
