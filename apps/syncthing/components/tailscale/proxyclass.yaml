apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: nishir-syncthing
spec:
  statefulSet:
    pod:
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/name: syncthing
              topologyKey: kubernetes.io/hostname
