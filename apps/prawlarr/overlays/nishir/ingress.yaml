apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prawlarr
  namespace: shikanime
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: prawlarr
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - prawlarr
