apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-proxy
spec:
  defaultBackend:
    service:
      name: matrix-proxy
      port:
        name: https
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-proxy-discord-media
spec:
  defaultBackend:
    service:
      name: matrix-proxy
      port:
        name: https
