apiVersion: v1
kind: Service
metadata:
  name: matrix-signal
  namespace: shikanime
spec:
  ports:
    - name: http
      port: 29328
      targetPort: http
  selector:
    app.kubernetes.io/name: matrix-signal
