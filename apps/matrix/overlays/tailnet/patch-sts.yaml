apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: matrix
  namespace: shikanime
spec:
  template:
    spec:
      containers:
        - name: synapse
          env:
            - name: CONDUIT_SERVER_NAME
              value: matrix.taila659a.ts.net
