apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: conduit-data
  namespace: shikanime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nishir-standard
  volumeMode: Filesystem
  volumeName: conduit-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mautrix-googlechat-data
  namespace: shikanime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nishir-standard
  volumeMode: Filesystem
