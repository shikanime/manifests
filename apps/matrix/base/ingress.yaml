apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix
  namespace: shikanime
spec:
  defaultBackend:
    service:
      name: matrix
      port:
        name: http
  tls:
    - hosts:
        - matrix
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-googlechat
  namespace: shikanime
spec:
  defaultBackend:
    service:
      name: matrix-googlechat
      port:
        name: http
  tls:
    - hosts:
        - matrix-googlechat
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-slack
  namespace: shikanime
spec:
  defaultBackend:
    service:
      name: matrix-slack
      port:
        name: http
  tls:
    - hosts:
        - matrix-slack
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-discord
  namespace: shikanime
spec:
  defaultBackend:
    service:
      name: matrix-discord
      port:
        name: http
  tls:
    - hosts:
        - matrix-discord
