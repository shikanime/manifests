apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mautrix-meta-tailscale
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mautrix-meta
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale-system
      ports:
        - port: http
