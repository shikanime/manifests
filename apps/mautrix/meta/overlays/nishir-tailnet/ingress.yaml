apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-meta
spec:
  defaultBackend:
    service:
      name: mautrix-meta
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - matrix-meta
