apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-meta
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: mautrix-meta
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - matrix-meta
