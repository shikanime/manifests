apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mautrix-meta
spec:
  defaultBackend:
    service:
      name: mautrix-meta
      port:
        name: http
  tls:
    - hosts:
        - matrix-meta
