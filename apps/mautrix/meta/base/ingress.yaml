apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muatrix-meta
spec:
  defaultBackend:
    service:
      name: muatrix-meta
      port:
        name: http
  tls:
    - hosts:
        - matrix-meta
