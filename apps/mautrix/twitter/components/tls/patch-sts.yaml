apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: matrix-twitter
spec:
  template:
    spec:
      containers:
        - name: mautrix
          volumeMounts:
            - name: certs
              mountPath: /etc/ssl/certs
              readOnly: true
