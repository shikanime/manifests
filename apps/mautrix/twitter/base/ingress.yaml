apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muatrix-twitter
spec:
  defaultBackend:
    service:
      name: muatrix-twitter
      port:
        name: http
  tls:
    - hosts:
        - matrix-twitter
