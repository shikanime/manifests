apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: muatrix-twitter-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: muatrix-twitter-data
