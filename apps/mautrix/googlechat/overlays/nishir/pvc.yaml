apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-googlechat-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: matrix-googlechat-data
