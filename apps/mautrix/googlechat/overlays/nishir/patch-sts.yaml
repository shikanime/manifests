apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: matrix-googlechat
spec:
  template:
    spec:
      volumes:
        - name: certs
          configMap:
            name: nishir-ca-certificates.crt
