apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: googlechat
  namespace: shikanime
  labels:
    app.kubernetes.io/name: googlechat
    app.kubernetes.io/part-of: googlechat
spec:
  endpoints:
    - interval: 60s
      path: /metrics
      port: metrics
      scrapeTimeout: 30s
  namespaceSelector:
    matchNames:
      - shikanime
  selector:
    matchLabels:
      app.kubernetes.io/instance: googlechat
      app.kubernetes.io/name: googlechat
