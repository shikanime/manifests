apiVersion: v1
kind: Service
metadata:
  name: matrix-googlechat
spec:
  publishNotReadyAddresses: true
  ports:
    - name: http
      port: 29320
      targetPort: http
  selector:
    app.kubernetes.io/name: matrix-googlechat
