apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-googlechat
spec:
  defaultBackend:
    service:
      name: matrix-googlechat
      port:
        name: http
  tls:
    - hosts:
        - matrix-googlechat
