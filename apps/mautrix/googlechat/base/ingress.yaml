apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muatrix-googlechat
spec:
  defaultBackend:
    service:
      name: muatrix-googlechat
      port:
        name: http
  tls:
    - hosts:
        - matrix-googlechat
