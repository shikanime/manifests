apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mautrix-googlechat
spec:
  defaultBackend:
    service:
      name: mautrix-googlechat
      port:
        name: http
  tls:
    - hosts:
        - matrix-googlechat
