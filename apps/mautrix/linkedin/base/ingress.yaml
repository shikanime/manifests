apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muatrix-linkedin
spec:
  defaultBackend:
    service:
      name: muatrix-linkedin
      port:
        name: http
  tls:
    - hosts:
        - matrix-linkedin
