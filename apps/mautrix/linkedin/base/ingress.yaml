apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mautrix-linkedin
spec:
  defaultBackend:
    service:
      name: mautrix-linkedin
      port:
        name: http
  tls:
    - hosts:
        - matrix-linkedin
