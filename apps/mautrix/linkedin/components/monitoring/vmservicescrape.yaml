apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: linkedin
  namespace: shikanime
  labels:
    app.kubernetes.io/name: linkedin
    app.kubernetes.io/part-of: linkedin
spec:
  endpoints:
    - interval: 60s
      path: /metrics
      port: metrics
      scrapeTimeout: 30s
  namespaceSelector:
    matchNames:
      - shikanime
  selector:
    matchLabels:
      app.kubernetes.io/instance: linkedin
      app.kubernetes.io/name: linkedin
