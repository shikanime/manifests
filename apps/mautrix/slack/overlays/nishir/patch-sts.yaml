apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mautrix-slack
spec:
  template:
    spec:
      containers:
        - name: mautrix
          volumeMounts:
            - name: certs
              mountPath: /etc/ssl/certs
              readOnly: true
      volumes:
        - name: certs
          configMap:
            name: nishir-ca-certificates.crt
