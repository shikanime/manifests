apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: muatrix-slack-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: muatrix-slack-data
