apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-slack
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: mautrix-slack
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - matrix-slack
