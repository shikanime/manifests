apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: matrix-slack-tailscale
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: matrix-slack
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale-system
      ports:
        - port: http
