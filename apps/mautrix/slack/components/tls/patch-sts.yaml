apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: matrix-slack
spec:
  template:
    spec:
      containers:
        - name: mautrix
          volumeMounts:
            - name: certs
              mountPath: /etc/ssl/certs
              readOnly: true
