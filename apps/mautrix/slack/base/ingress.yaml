apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-slack
spec:
  defaultBackend:
    service:
      name: mautrix-slack
      port:
        name: http
  tls:
    - hosts:
        - matrix-slack
