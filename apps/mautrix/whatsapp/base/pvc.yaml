apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mautrix-whatsapp-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  volumeMode: Filesystem
