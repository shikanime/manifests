apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mautrix-whatsapp
spec:
  defaultBackend:
    service:
      name: mautrix-whatsapp
      port:
        name: http
  tls:
    - hosts:
        - matrix-whatsapp
