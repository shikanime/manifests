apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muatrix-whatsapp
spec:
  defaultBackend:
    service:
      name: muatrix-whatsapp
      port:
        name: http
  tls:
    - hosts:
        - matrix-whatsapp
