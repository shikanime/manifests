apiVersion: v1
kind: Service
metadata:
  name: muatrix-whatsapp
spec:
  publishNotReadyAddresses: true
  ports:
    - name: http
      port: 29318
      targetPort: http
  selector:
    app.kubernetes.io/name: muatrix-whatsapp
