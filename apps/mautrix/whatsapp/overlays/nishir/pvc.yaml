apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mautrix-whatsapp-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: mautrix-whatsapp-data
