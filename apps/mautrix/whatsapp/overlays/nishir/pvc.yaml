apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: muatrix-whatsapp-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: muatrix-whatsapp-data
