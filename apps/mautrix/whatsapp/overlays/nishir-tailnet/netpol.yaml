apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: muatrix-whatsapp-tailscale
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: muatrix-whatsapp
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale-system
      ports:
        - port: http
