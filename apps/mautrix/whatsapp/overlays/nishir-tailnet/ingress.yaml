apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-whatsapp
spec:
  defaultBackend:
    service:
      name: mautrix-whatsapp
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - matrix-whatsapp
