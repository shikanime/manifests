apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-signal
spec:
  defaultBackend:
    service:
      name: matrix-signal
      port:
        name: http
  tls:
    - hosts:
        - matrix-signal
