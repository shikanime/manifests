apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muatrix-signal
spec:
  defaultBackend:
    service:
      name: muatrix-signal
      port:
        name: http
  tls:
    - hosts:
        - matrix-signal
