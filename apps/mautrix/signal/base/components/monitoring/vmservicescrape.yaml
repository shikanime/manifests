apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: mautrix-signal
  namespace: shikanime
  labels:
    app.kubernetes.io/name: mautrix-signal
    app.kubernetes.io/part-of: mautrix-signal
spec:
  endpoints:
    - interval: 60s
      path: /metrics
      port: http
      scrapeTimeout: 30s
  namespaceSelector:
    matchNames:
      - shikanime
  selector:
    matchLabels:
      app.kubernetes.io/instance: mautrix-signal
      app.kubernetes.io/name: mautrix-signal