apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - namereference.yaml
patches:
  - path: patch-ingress.yaml
  - path: patch-sts.yaml
  - path: patch-svc.yaml
resources:
  - ../default
  - cert.yaml
secretGenerator:
  - envs:
      - sonarr/.env
    name: sonarr-pkcs12-password
