apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: shikanime
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/tags: "tag:sonarr"
spec:
  ports:
    - port: 8989
      targetPort: http
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: sonarr
