apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: shikanime
spec:
  ports:
    - port: 8989
      targetPort: http
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/name: sonarr
