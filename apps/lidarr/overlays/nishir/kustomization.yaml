apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - namereference.yaml
resources:
  - ../default
  - cert.yaml
secretGenerator:
  - envs:
      - lidarr/.env
    name: lidarr-pkcs12-password
  - files:
      - lidarr/config.xml
    name: lidarr-pre-config
