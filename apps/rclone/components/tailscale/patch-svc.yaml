apiVersion: v1
kind: Service
metadata:
  name: ftp
  namespace: shikanime
  annotations:
    tailscale.com/hostname: ftp
    tailscale.com/proxy-class: nishir-rclone
    tailscale.com/tags: tag:ftp
spec:
  loadBalancerClass: tailscale
