apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rclone-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: rclone-cache
