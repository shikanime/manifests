apiVersion: v1
kind: Service
metadata:
  name: rclone
  namespace: shikanime
spec:
  loadBalancerClass: tailscale
  ports:
    - name: nfs
      port: 2049
      targetPort: nfs
  selector:
    app.kubernetes.io/name: rclone
  type: LoadBalancer
