apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rclone
  namespace: shikanime
spec:
  defaultBackend:
    service:
      name: s3
      port:
        name: http
  tls:
    - hosts:
        - s3
