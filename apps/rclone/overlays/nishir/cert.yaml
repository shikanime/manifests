apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rclone-tls
spec:
  commonName: rclone
  dnsNames:
    - rclone
  issuerRef:
    kind: ClusterIssuer
    name: nishir
  secretName: rclone-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
