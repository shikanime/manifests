apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches:
  - path: patch-deploy.yaml
resources:
  - ../default
  - cert.yaml
secretGenerator:
  - envs:
      - rclone-ftp/.env
    name: rclone-ftp
  - files:
      - rclone-htpasswd/.env
    name: rclone-htpasswd
