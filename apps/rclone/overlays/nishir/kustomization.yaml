apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: shikanime/studio
components:
  - ../../components/tls
resources:
  - ../../base
  - cert.yaml
patches:
  - path: patch-sts.yaml
secretGenerator:
  - envs:
      - rclone-ftp/.env
    name: rclone-ftp
  - envs:
      - rclone-htpasswd/.env
    name: rclone-htpasswd
