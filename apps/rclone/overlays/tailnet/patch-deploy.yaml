apiVersion: apps/v1
kind: Deployment
metadata:
  name: rclone
spec:
  template:
    spec:
      containers:
        - name: rclone-ftp
          env:
            - name: RCLONE_PUBLIC_IP
              value: "100.107.207.113"
