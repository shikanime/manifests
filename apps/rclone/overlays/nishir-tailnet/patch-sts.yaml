apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: rclone
spec:
  template:
    spec:
      containers:
        - name: rclone-ftp
          env:
            - name: RCLONE_PUBLIC_IP
              value: "100.102.246.5"
