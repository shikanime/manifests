apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bazarr
spec:
  template:
    spec:
      containers:
        - name: bazarr
          volumeMounts:
            - name: certs
              mountPath: /etc/ssl/certs
              readOnly: true
