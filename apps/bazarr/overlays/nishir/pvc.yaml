apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-config
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: bazarr-config
