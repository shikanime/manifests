apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: shikanime
patches:
- path: patch-sts.yaml
resources:
  - ../../base
  - netpol.yaml
  - pvc.yaml
secretGenerator:
  - envs:
      - metatube/.enc.env
    name: metatube
