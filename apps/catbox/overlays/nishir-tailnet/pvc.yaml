apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catbox-workspaces
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 64Gi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: catbox-workspaces
