apiVersion: v1
kind: Service
metadata:
  name: catbox
spec:
  type: LoadBalancer
  ports:
    - name: ssh
      port: 22
      targetPort: ssh
    - name: mdns
      protocol: UDP
      port: 5353
      targetPort: mdns
  selector:
    app.kubernetes.io/name: catbox
