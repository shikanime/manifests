apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: catbox
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: catbox
      app.kubernetes.io/name: catbox
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: ssh
