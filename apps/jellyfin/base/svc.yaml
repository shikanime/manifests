apiVersion: v1
kind: Service
metadata:
  name: jellyfin
spec:
  ports:
    - name: http
      port: 8096
      targetPort: http
  selector:
    app.kubernetes.io/name: jellyfin
---
apiVersion: v1
kind: Service
metadata:
  name: metatube
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: metatube
