apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - namereference.yaml
components:
  - ../../components/v4l
  - ../../components/tls
patches:
  - path: patch-sts.yaml
resources:
  - ../../base
  - cert.yaml
secretGenerator:
  - envs:
      - jellyfin/.env
    name: jellyfin-pkcs12-password
  - envs:
      - metatube/.env
    name: metatube
