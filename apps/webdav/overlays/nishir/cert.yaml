apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rclone-tls
spec:
  secretName: rclone-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  commonName: webdav
  dnsNames:
    - webdav
