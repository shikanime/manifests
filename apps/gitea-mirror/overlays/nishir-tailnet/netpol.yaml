---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: gitea-mirror-tailscale
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: gitea-mirror
      app.kubernetes.io/name: gitea-mirror
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale-system
      ports:
        - port: http
