apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gitea-mirror
spec:
  template:
    spec:
      containers:
        - name: gitea-mirror
          envFrom:
            - secretRef:
                name: gitea-mirror-oidc
