apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whisparr
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: whisparr
      port:
        name: https
  ingressClassName: tailscale
  tls:
    - hosts:
        - whisparr
