apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whisparr-tls
spec:
  commonName: whisparr
  dnsNames:
    - whisparr
  issuerRef:
    name: nishir
    kind: ClusterIssuer
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        name: whisparr-pkcs12-password
        key: password
      profile: Modern2023
  secretName: whisparr-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
