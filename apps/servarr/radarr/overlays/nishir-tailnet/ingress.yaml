apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: radarr
      port:
        name: https
  tls:
    - hosts:
        - radarr
  ingressClassName: tailscale
