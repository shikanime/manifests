apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-tls
spec:
  commonName: radarr
  dnsNames:
    - radarr
  issuerRef:
    name: nishir
    kind: ClusterIssuer
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        name: radarr-pkcs12-password
        key: password
      profile: Modern2023
  secretName: radarr-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
