apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lidarr-tls
spec:
  commonName: lidarr
  dnsNames:
    - lidarr
  issuerRef:
    name: nishir
    kind: ClusterIssuer
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        name: lidarr-pkcs12-password
        key: password
      profile: Modern2023
  secretName: lidarr-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
