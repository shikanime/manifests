apiVersion: v1
kind: Service
metadata:
  name: adguardhome-admin
  namespace: shikanime
spec:
  ports:
    - name: http
      port: 80
      targetPort: http-admin
  selector:
    app.kubernetes.io/name: adguardhome
