apiVersion: apps/v1
kind: Deployment
metadata:
  name: adguardhome
  namespace: shikanime
spec:
  template:
    spec:
      containers:
        - name: adguardhome
          ports:
            - containerPort: 3000
              name: http-admin
