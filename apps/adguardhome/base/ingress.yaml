apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguardhome
  namespace: shikanime
spec:
  defaultBackend:
    service:
      name: adguardhome
      port:
        name: http
  tls:
    - hosts:
        - adguardhome
