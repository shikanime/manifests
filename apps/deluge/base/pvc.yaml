apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deluge-config
  namespace: shikanime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: longhorn
  volumeMode: Filesystem
  volumeName: deluge-config
