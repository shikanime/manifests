apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synapse-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 16Gi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: synapse-data
