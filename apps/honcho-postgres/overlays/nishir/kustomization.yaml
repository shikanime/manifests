---
resources:
  - ../../base
namespace: shikanime
patches:
  - path: patch-pvc.yaml
secretGenerator:
  - name: honcho-postgres
    type: basic-auth
    envs:
      - honcho-postgres/.enc.env
    options:
      disableNameSuffixHash: true
