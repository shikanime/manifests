apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vaultwarden
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: vaultwarden
      port:
        name: https
  tls:
    - hosts:
        - vaultwarden
  ingressClassName: tailscale
