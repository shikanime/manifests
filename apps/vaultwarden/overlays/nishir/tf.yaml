apiVersion: infra.contrib.fluxcd.io/v1alpha2
kind: Terraform
metadata:
  name: vaultwarden
  namespace: shikanime
spec:
  approvePlan: auto
  interval: 30m
  path: ./modules/vaultwarden-token
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  values:
    name: vaultwarden
    namespace: shikanime
