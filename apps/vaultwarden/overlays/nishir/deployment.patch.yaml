apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaultwarden
  namespace: shikanime
spec:
  template:
    spec:
      containers:
        - name: vaultwarden
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 10m
              memory: 32Mi
