apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: shikanime
spec:
  ports:
    - name: https
      port: 80
      targetPort: https
  selector:
    app.kubernetes.io/name: vaultwarden
