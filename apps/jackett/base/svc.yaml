apiVersion: v1
kind: Service
metadata:
  name: jackett
  namespace: shikanime
spec:
  ports:
    - port: 9117
      targetPort: http
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: jackett
