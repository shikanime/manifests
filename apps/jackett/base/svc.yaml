apiVersion: v1
kind: Service
metadata:
  name: jackett
  namespace: shikanime
  annotations:
    tailscale.com/hostname: jackett
    tailscale.com/expose: "true"
    tailscale.com/tags: "tag:jackett"
spec:
  ports:
    - port: 9117
      targetPort: http
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/name: jackett
