apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jackett
  namespace: shikanime
  annotations:
    tailscale.com/tags: tag:web
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: Jackett
spec:
  rules:
    - host: jackett.taila659a.ts.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jackett
                port:
                  name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - jackett
