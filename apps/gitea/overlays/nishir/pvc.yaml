apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-data
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 8Gi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: gitea-data
