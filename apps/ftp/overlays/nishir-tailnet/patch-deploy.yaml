apiVersion: apps/v1
kind: Deployment
metadata:
  name: ftp
spec:
  template:
    spec:
      containers:
        - name: rclone
          env:
            - name: RCLONE_PUBLIC_IP
              value: "100.102.246.5"
