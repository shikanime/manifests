apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: shikanime
configMapGenerator:
  - files:
      - synapse-proxy/Caddyfile
    name: synapse-proxy
patches:
  - path: patch-ingress.yaml
resources:
  - ../nishir
  - netpol.yaml
  - proxyclass.yaml
