apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: synapse-proxy-tls
spec:
  commonName: synapse
  dnsNames:
    - synapse-proxy
  issuerRef:
    kind: ClusterIssuer
    name: nishir
  secretName: synapse-proxy-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
