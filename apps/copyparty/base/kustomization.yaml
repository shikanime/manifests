resources:
  - pvc.yaml
  - sts.yaml
  - svc.yaml
  - vpa.yaml
metadata:
  annotations:
    automata.shikanime.studio/images:
      '[ { "name": "copyparty", "tag-regex": "^(?P<version>.+)$" }, { "name":
      "rclone", "tag-regex": "^(?P<version>\\d+\\.\\d+\\.\\d+)$" } ]'
images:
  - name: copyparty
    newName: copyparty/dj
    newTag: 1.20.16
  - name: rclone
    newName: rclone/rclone
    newTag: 1.74.3
configMapGenerator:
  - name: copyparty-rclone
    namespace: shikanime
    files:
      - rclone/rclone.conf
