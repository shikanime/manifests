apiVersion: v1
kind: Service
metadata:
  name: copyparty
spec:
  ports:
    - name: ftp
      port: 3921
      targetPort: ftp
    - name: http
      port: 3923
      targetPort: http
  selector:
    app.kubernetes.io/name: copyparty
