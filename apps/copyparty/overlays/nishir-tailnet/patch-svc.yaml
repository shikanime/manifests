apiVersion: v1
kind: Service
metadata:
  name: ftp
  annotations:
    tailscale.com/hostname: ftp
    tailscale.com/proxy-class: copyparty
    tailscale.com/tags: tag:ftp
spec:
  loadBalancerClass: tailscale
