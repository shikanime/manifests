apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: copyparty-config
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: copyparty-config
