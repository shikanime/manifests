apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: copyparty-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: copyparty-data
