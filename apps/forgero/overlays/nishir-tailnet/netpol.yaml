apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: forgero-tailscale
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: forgero
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale-system
      ports:
        - port: http
        - port: ssh
