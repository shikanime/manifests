apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-cache
  namespace: shikanime
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 256Gi
  storageClassName: nishir-local
