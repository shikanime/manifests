apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisparr-config
  namespace: shikanime
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 4Gi
  storageClassName: nishir-standard
  volumeMode: Filesystem
  volumeName: whisparr-config
