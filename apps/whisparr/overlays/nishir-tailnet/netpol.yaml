apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: whisparr-tailscale
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: whisparr
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale-system
      ports:
        - port: https
          protocol: TCP
