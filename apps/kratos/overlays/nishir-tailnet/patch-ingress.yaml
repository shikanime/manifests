apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: accounts
  annotations:
    tailscale.com/proxy-class: accounts
    tailscale.com/tags: tag:web
spec:
  ingressClassName: tailscale
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kratos
  annotations:
    tailscale.com/proxy-class: kratos
    tailscale.com/tags: tag:web
spec:
  ingressClassName: tailscale
