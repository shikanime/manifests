apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kratos-postgres-data
  namespace: shikanime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nishir-standard
  volumeMode: Filesystem
  volumeName: kratos-postgres-data
