apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prowlarr-tls
spec:
  commonName: prowlarr
  dnsNames:
    - prowlarr
  issuerRef:
    name: nishir
    kind: ClusterIssuer
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        name: prowlarr-pkcs12-password
        key: password
      profile: Modern2023
  secretName: prowlarr-tls
  usages:
    - digital signature
    - key encipherment
    - server auth
