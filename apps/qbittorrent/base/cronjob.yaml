apiVersion: batch/v1
kind: CronJob
metadata:
  name: qbittorrent-resume
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - command:
                - /bin/bash
                - /scripts/qbittorrent-resume.sh
              image: qbittorrent
              envFrom:
                - secretRef:
                    name: qbittorrent
              name: qbittorrent-resume
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
          restartPolicy: OnFailure
          volumes:
            - name: scripts
              configMap:
                name: qbittorrent-scripts
                defaultMode: 0755
