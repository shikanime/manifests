apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: seerr
spec:
  template:
    spec:
      containers:
        - name: seerr
          volumeMounts:
            - name: certs
              mountPath: /etc/ssl/certs
              readOnly: true
