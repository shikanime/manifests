apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seerr
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: seerr
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - seerr
