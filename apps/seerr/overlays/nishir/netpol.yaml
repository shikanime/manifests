---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: seerr
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: seerr
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: jellyfin
      ports:
        - port: http
