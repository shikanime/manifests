apiVersion: v1
kind: Service
metadata:
  name: oceando
  annotations:
    tailscale.com/hostname: oceando
    tailscale.com/proxy-class: oceando
    tailscale.com/tags: tag:infra
spec:
  loadBalancerClass: tailscale
