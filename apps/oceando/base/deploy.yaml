apiVersion: apps/v1
kind: Deployment
metadata:
  name: oceando
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: oceando
  template:
    metadata:
      labels:
        app.kubernetes.io/name: oceando
    spec:
      securityContext:
        fsGroup: 100
      containers:
        - image: oceando
          livenessProbe:
            initialDelaySeconds: 30
            tcpSocket:
              port: ssh
          name: oceando
          ports:
            - containerPort: 22
              name: ssh
            - containerPort: 5353
              name: mdns
              protocol: UDP
          readinessProbe:
            initialDelaySeconds: 10
            tcpSocket:
              port: ssh
          securityContext:
            privileged: true
          startupProbe:
            failureThreshold: 30
            initialDelaySeconds: 3
            tcpSocket:
              port: ssh
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /sys/kernel/debug
              name: sys-kernel-debug
            - mountPath: /sys/kernel/tracing
              name: sys-kernel-tracing
            - mountPath: /home/shika/.config/sops
              name: sops-config
      hostname: oceando
      volumes:
        - hostPath:
            path: /sys/kernel/debug
            type: Directory
          name: sys-kernel-debug
        - hostPath:
            path: /sys/kernel/tracing
            type: Directory
          name: sys-kernel-tracing
        - hostPath:
            path: /Users/shikanimedeva/.config/sops
            type: Directory
          name: sops-config
