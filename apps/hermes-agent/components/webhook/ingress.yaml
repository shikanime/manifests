---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: automata-webhook
spec:
  defaultBackend:
    service:
      name: hermes-agent
      port:
        name: webhook
  tls:
    - hosts:
        - automata-webhook
