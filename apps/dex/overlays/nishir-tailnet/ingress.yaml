apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dex
  annotations:
    tailscale.com/tags: tag:web
spec:
  defaultBackend:
    service:
      name: dex
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - dex
