apiVersion: v1
kind: Service
metadata:
  name: dex
spec:
  ports:
    - name: http
      port: 5556
      targetPort: http
  selector:
    app.kubernetes.io/instance: dex
    app.kubernetes.io/name: dex
