apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 2Gi
  storageClassName: nishir-fast
  volumeMode: Filesystem
  volumeName: radarr-config
