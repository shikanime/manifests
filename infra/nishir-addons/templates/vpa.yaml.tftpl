apiVersion: v1
kind: Namespace
metadata:
  name: vpa
---
apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: vpa
  namespace: kube-system
spec:
  repo: ${repo}
  chart: ${chart}
  targetNamespace: vpa
  version: ${version}
  helmVersion: "v3"
  bootstrap: false
  failurePolicy: abort
